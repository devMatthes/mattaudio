@page
@model mattaudio.Pages.Tracks.CreateModel

@{
    ViewData["Title"] = "Add";
}

<h1>Add</h1>

<h4>Track</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Track.Artist" class="control-label"></label>
                <input asp-for="Track.Artist" class="form-control" />
                <span asp-validation-for="Track.Artist" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Track.Title" class="control-label"></label>
                <input asp-for="Track.Title" class="form-control" />
                <span asp-validation-for="Track.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
